Replaces the Layout Sundeadkeys or "German (with Sun dead keys)" with basic + åÅ on üÜ 

run command:
setxkbmap de sundeadkeys
only works until reboot.

Add this line to a layout:
key <AD11>	{ [ udiaeresis, Udiaeresis,            aring,   Aring ]	};
